using UnityEngine;
using System.Collections;

public class bestrightflip : MonoBehaviour {
void start(){
		Input.GetAxis("Vertical");
		// and...
		Input.GetAxis("Horizontal");
		}
	// Update is called once per frame
	void Update () {
		

	}
}
